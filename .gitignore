# Dependencies
node_modules/
dist/
build/
.env

# Large Assets
*.pdf
*.jpg
*.jpeg
*.png
*.zip
*.jar

# Keep some assets if needed (example)
# !src/assets/*.png

# Database and Uploads
server/dev.db
server/thumbnails/
server/uploads/
unpacked/

# System Files
.DS_Store
Thumbs.db

# IDEs
.idea/
*.iml
.vscode/

# Build and Temp
dist/
build/
dist_electron/
unpacked/
scratch/
out/

# Database and Uploads
server/prisma/dev.db
server/dev.db
server/thumbnails/
server/uploads/

# APK 배포 파일 (바이너리, 용량 큼)
server/update/*.apk
server/update/*.idsig
*.apk
*.idsig

# 임시/프롬프트 파일
bootanimation_prompt.txt

# Android 빌드 결과물
android/app/build/
android/.gradle/
android/local.properties
android_mobile/.gradle/
android_mobile/app/build/
android_mobile/local.properties

# 서버 로그 (런타임 생성)
server-stdout.log
server-stderr.log
gradle-build.log

# 임시 스크립트
server/query_db.js

# 배포 메타 (로컬 상태)
server/update/deploy-meta.json

# Agentic / IDE state
.claude/
cloudflareTokenKey.md

# 서버 접속 정보·계정·인프라 인벤토리 포함 → 공개 저장소에 올리지 않음
사이니지프로_사용자_매뉴얼.md
인수인계.md
개발_인수인계.md

# 하드웨어 참고자료 (제품 소스 아님)
rk3566.txt

# 샘플 미디어 (대용량 mp4/png, 제품 소스 아님)
sample_media/
# 부트애니 생성물(38MB zip / 6MB bmp) — 저장소 비대화 방지.
#   zip: 전역 *.zip 규칙에 이미 걸림. build_bootanim.py + source_*.mp4 로 재생성.
#   bmp: 애니 첫 프레임(part0/0000.png)을 1920x1080 24bit BMP 로 뽑은 것.
# ※ OEM_backup_*.zip 은 공장 기본 부트애니 백업(재생성 불가) → git add -f 로 커밋함.
branding/bootanimation/custom_logo.bmp
