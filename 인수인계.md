# SignagePro 인수인계 문서 (HANDOVER)

> 목적: 담당자 부재(퇴사/교체) 시에도 **제주 오설록 디지털 사이니지 시스템을 운영·복구·확장**할 수 있도록 인프라·절차·자산 위치를 정리한다.
> 성격: **살아있는 문서** — 구성이 바뀔 때마다 갱신. (작성 시작: 2026-06-14)
>
> ⚠️ **범위**: 운영 인프라(현장 NAS·현장 백업 PC·STB·네트워크·코드/서명키·절차).
> **제외**: 개발용 PC *하드웨어*는 담당자 개인 소유 → 인계 대상 아님. **단, 그 PC 안의 소스코드·서명키(keystore)·빌드환경은 회사 자산으로 반드시 인계**(§9 참조).

---

## 1. 시스템 한눈에 보기

SignagePro = 매장 디지털 사이니지(광고/메뉴 화면) 운영 시스템. 3개 축:

| 구성 | 역할 | 기술 |
|---|---|---|
| **CMS(서버+대시보드)** | 미디어 업로드·재생목록 편성·스케줄·기기 관제 | Node/Express + SQLite + Socket.io + React 대시보드 |
| **STB(셋톱박스)** | 각 화면에 연결돼 콘텐츠 재생 | Android 네이티브 플레이어(`com.signagepro.player`, ExoPlayer) |
| **NAS / 백업** | 운영 서버 호스팅 + 미디어/DB 저장 + 백업 | Synology(예정) + 현장 백업 PC |

용어: **CMS**=콘텐츠 관리 시스템(머리), **STB**=셋톱박스 재생기(손발), **토폴로지**=전체 연결 구조.

---

## 2. 토폴로지 (목표 구조)

```
[현장 매장]
  ┌─ NAS = 운영 서버(Docker) + 미디어 + DB   ← STB와 같은 LAN
  │        │ 일일 스냅샷 백업(단방향)
  │        ▼
  └─ 데스크탑 PC = 로컬 백업 (항상 켜짐)
         ▲ STB(셋톱박스) 5대/4대 ── LAN/Tailscale ──┘

[다른 장소]
  └─ 개발 PC = 개발 + 오프사이트 백업 (개인 소유)

keystore(앱 서명키) = 위 + USB 별도 1벌
전부 Tailscale(사설 VPN)로 연결. 멀티사이트 확장 시 현장마다 최소형 NAS(로컬 미디어 캐시).
```

- 자세한 배포 토폴로지/포트: [DEPLOYMENT.md](DEPLOYMENT.md)
- 보안 현황: [SECURITY.md](SECURITY.md)
- 백업 계획: [BACKUP_PLAN.md](BACKUP_PLAN.md)

> ⏳ **현 상태(2026-06-14)**: 위는 *목표* 구조. 현재 운영 서버는 **현장 데스크탑 PC**(`C:\signagepro`)에서 동작 중이며, **NAS 도입·서버 NAS 이전은 예정**. R2(클라우드)는 dev/staging 제거 완료, 현장 전환 예정.

---

## 3. 인프라 인벤토리

> 실제 IP·계정주·시리얼 등은 확정/구매 후 채운다.

| 장비 | 위치 | 역할 | 식별자 | 비고 |
|---|---|---|---|---|
| 현장 운영 서버 | 매장(jeju-osuloc) | CMS 서버(현 데스크탑 → 향후 NAS) | Tailscale `100.122.76.95` / `jeju-osuloc:3300` | `C:\signagepro` |
| 현장 NAS | 매장 | 서버+미디어+DB (예정) | [구매 후] | Synology DS224+ 권장 |
| 현장 백업 PC | 매장 | 로컬 백업 | [채울 것] | 항상 켜짐 |
| STB ×5 (매장A) | [매장A] | 재생기 | dev-201 … | 큐버/크라이저 |
| STB ×4 (매장B) | [매장B] | 재생기 | dev-202 … | |
| 개발 PC | 개인 | 개발·빌드·오프사이트 백업 | (개인) | **하드웨어 인계 제외** |
| Tailnet | — | 사설 VPN | `tailafea2d.ts.net` | MagicDNS |

---

## 4. 접속·계정 (실제 비밀값은 §10 안전 인계로)

| 대상 | 접속 방법 | 계정/위치 |
|---|---|---|
| 현장 서버 원격 | **AnyDesk** + **Tailscale** | [AnyDesk ID/비번, Tailscale 계정] |
| Tailscale(사설망) | tailscale.com 로그인 → tailnet | [계정 소유자/이메일] |
| NAS 관리자 | DSM 웹(로컬/Tailscale) | [관리자 계정] |
| 대시보드(CMS) | 브라우저 → 서버:포트 | adminPassword(§10) |
| STB 원격 | Tailscale + **ADB**(`adb connect <tailscale-ip>:5555`) | 루팅 기기 |

> ⚠️ Tailscale 계정이 **담당자 개인 이메일**로 묶여 있으면 인계 불가 위험 → **회사/공용 이메일**로 tailnet 소유권 이전 필요(§11 TODO).

---

## 5. 일상 운영 (대시보드)

1. **미디어 업로드**: 대시보드 → 미디어 스케줄링 → 에셋 라이브러리 업로드(로컬 `/uploads` 저장)
2. **재생목록 편성**: 기기별 타임라인에 드래그 → 전환/표시시간 설정 → **변경사항 저장 및 배포**
3. **기기 관제**: 대시보드(관제) → 실시간 상태/CPU/메모리/스크린, 전원·볼륨·재부팅 제어
4. **PDF→이미지 변환**: `이미지원본/PDF를PNG로변환.bat` (흰여백 자동 크롭, 1920×1080 유지)

---

## 6. 백업·복구

- **무엇을**: DB(`server/prisma/dev.db`) · `.env` · **keystore**(서명키, 대체 불가) · `uploads`(미디어). 코드는 git.
- **방법**: SQLite는 `.backup` 스냅샷(단방향), 미디어/시크릿은 robocopy. 상세 → [BACKUP_PLAN.md](BACKUP_PLAN.md).
- **복구 테스트**: 분기 1회 — 백업 DB로 서버 기동되는지 확인.
- 🔴 **keystore 분실 = 기존 앱 OTA 업데이트 영구 불가** → 다중·오프사이트·USB 보관.

---

## 7. 배포 파이프라인 / OTA

- **dev → staging → 운영(main)** 순. 운영 자율 배포 금지, 사람이 AnyDesk로 적용. → [DEPLOYMENT.md](DEPLOYMENT.md)
- **APK 빌드**: 개발 PC(Gradle). 빌드된 APK를 서버가 OTA로 배포 → STB가 `pm install`/Device Owner로 무인 설치.
- 버전 규칙: versionCode/Name +1, **자율 배포 금지**(메모리 규칙).

---

## 8. 장애 대응 (빠른 참고)

| 증상 | 1차 확인 |
|---|---|
| STB 화면 안 나옴/검은화면 | HDMI·전원, 서버 연결, STB 재부팅(원격 ADB) — 캐시 재생되므로 서버 잠깐 꺼져도 재생은 지속 |
| 특정 이미지만 안 보임 | 미디어 캐시/파일 문제 → 그룹 재배정으로 재다운로드 |
| 서버 다운 | NAS/PC 재시작, DB·uploads 무결성, 최후엔 백업 복구 |
| 원격 접속 불가 | Tailscale 상태, AnyDesk, 공유기/ISP |

---

## 9. 🔑 소스코드·서명키 인계 (가장 중요 — 개인 PC라도 회사 자산)

후임자가 **개발·빌드·업데이트를 이어가려면** 아래가 반드시 인계돼야 한다:

- [ ] **소스코드** — git 원격 `https://github.com/5sherlock/signagepro` (현재 **개인 GitHub 계정**). 코드는 PC 외에 GitHub에도 있어 양호하나, **개인 계정 종속** → 회사 조직(org)으로 이전 또는 후임자 접근권 부여 필요.
- [ ] **서명키(keystore)** + 키 암호 — APK 서명용, 대체 불가
- [ ] **빌드 환경** — Android SDK/Gradle 버전, build 스크립트(`build_apk.ps1` 등)
- [ ] **.env 시크릿** — 환경별 값(§10)
- [ ] **문서 일체** — 본 문서 + DEPLOYMENT/SECURITY/BACKUP_PLAN 등

---

## 10. 비밀값 안전 인계 (이 문서에 평문 금지)

DEVICE_SECRET · adminPassword · keystore 암호 · NAS/AnyDesk/Tailscale 계정 등 **실제 값은 본 문서에 적지 않는다.**
인계 시 **비밀번호 관리자(1Password/Bitwarden 등) 공유** 또는 **봉인 문서**로 별도 전달.
> 참고: 현재 일부 시크릿이 환경 간 공유·약함(`signagepro-secret-2026`) → 인계 전 **환경별 강한 값으로 교체 권장**(SECURITY.md §1).

---

## 11. 외부 업체·연락처

| 구분 | 업체/담당 | 연락처 |
|---|---|---|
| STB | 큐버(QUBER) 박석영 차장 | sypark@quber.net / 010-8377-4117 |
| NAS | Synology(총판/리셀러) | [채울 것] |
| 도메인 | [등록업체] | signage-pro.com (유지/이전 결정 중) |
| ISP/회선 | [통신사] | [채울 것] |

---

## 12. TODO (확정되면 본 문서 채우기)

- [ ] NAS 구매·설치 → 인벤토리(§3) IP·시리얼 기입
- [ ] 운영 서버 NAS 이전(컨테이너화) 후 토폴로지 "현 상태" 갱신
- [ ] 현장 R2 → 로컬 마이그레이션 + R2 키 폐기 + Cloudflare 정리
- [ ] **Tailscale 소유권을 회사/공용 계정으로 이전**(개인 이메일 종속 해소)
- [ ] **소스코드 회사 접근 git 원격 확보** (개인 PC 단일 보관 해소)
- [ ] 환경별 강한 비밀값 교체(SECURITY.md §1)
- [ ] 백업 스크립트 배치 + Task Scheduler 등록 + 복구 테스트
- [ ] 매장A/B STB 식별자·위치 표 정리
- [ ] **운영 배포 시 STB `server_url`을 운영서버(NAS) MagicDNS 이름으로 설정** — *개발 중엔 dev PC IP가 정상*(MagicDNS는 현장 서버용이라 dev에선 연동 안 됨). 운영 전환 때 IP→이름으로 바꿔 IP 종속·소유권 이전 리스크 해소.

---

## 관련 문서
- [DEPLOYMENT.md](DEPLOYMENT.md) — 배포 토폴로지/포트/파이프라인
- [SECURITY.md](SECURITY.md) — 보안 점검·조치
- [BACKUP_PLAN.md](BACKUP_PLAN.md) — 백업 3-2-1 계획
- [큐버_STB_견적요청.md](큐버_STB_견적요청.md) — STB 구매 사양/견적
