# Synology NAS 구매 가이드 (SignagePro 운영 서버용)

> **이 문서를 제미나이/구매 담당에게 그대로 전달하세요.** 디지털 사이니지 운영 서버로 쓸 NAS를 사려는데, 쿠팡 등에서 가격이 천차만별이고 "디스크 없음" 같은 표기가 헷갈려서 정리한 가이드입니다.

---

## 1. 이 NAS로 무엇을 하나 (용도)
- 매장 디지털 사이니지(광고 화면) 시스템의 **운영 서버**.
- NAS에서 **Docker로 Node.js 서버 + DB(SQLite)** 를 돌리고, **미디어 파일(이미지/영상)을 저장·서빙**.
- 같은 매장 LAN의 **셋톱박스(STB) 4~5대**가 NAS에서 콘텐츠를 받아 재생. 향후 **20대까지 확장** 가능.
- **4K 콘텐츠** 운영 예정. 원격 관리는 **Tailscale(VPN)**.
- 즉 단순 파일저장이 아니라 **"작은 서버 + 미디어 스토리지"** 로 쓴다 → CPU·RAM·Docker가 중요.

---

## 2. 결론 — 사야 할 것은 **3가지** (따로 산다)
NAS는 보통 **본체(껍데기)만** 팔고 디스크·메모리는 별도입니다. 아래 3개를 각각 구매:

| # | 품목 | 권장 | 비고 |
|---|---|---|---|
| 1 | **NAS 본체** | **Synology DS225+** (디스크 미포함/diskless) | Intel/x86 CPU·Docker(Container Manager) 지원 모델이어야 함 |
| 2 | **하드디스크 2개** | NAS 전용 HDD (WD Red Plus 또는 Seagate IronWolf) **4TB × 2**, RAID 1(미러) | 24시간용. 일반 데스크탑 HDD 금지 |
| 3 | **메모리(RAM) 증설** | 4GB 모듈 추가 (기본 2GB → **6GB**) | Docker로 서버 돌리려면 필요 |

> 합계 예산 가늠(대략, 시세 변동): 본체 + 디스크2 + RAM = **대략 80만~120만원대.** (정확한 금액은 구매 시점 시세로 확인)

---

## 3. 본체 모델 — DS225+ (DS224+ 후속, 2베이 Plus급)

> ⚠️ **DS225+ 사양은 구매 전 시놀로지 공식 사양표로 반드시 확인.** 아래 수치(CPU 모델·RAM 최대)는 전작 DS224+ 기준 참고치이며, 신형은 다를 수 있음. **모델명보다 "조건 충족"이 핵심.**

사기 전 **반드시 3가지 확인** (이게 진짜 게이팅 조건):
1. **Intel/x86 CPU + Docker(Container Manager) 지원** — ARM 모델·`DS○○j` 시리즈는 Docker 안 됨 → **부적합.** Node 서버를 컨테이너로 돌려야 하므로 필수.
2. **3rd-party 디스크 호환** — 일부 신형 시놀로지는 자사 브랜드 디스크만 요구할 수 있음 → WD/Seagate 못 쓸 수 있으니 **호환 목록 확인.**
3. **RAM 증설 가능** 여부 + 최대 용량 — Docker 서버용으로 충분한지.

- 참고(전작 DS224+): 2베이, Intel Celeron J4125(4코어), 기본 2GB(증설 가능, 공식 최대 6GB), 기가비트 LAN, Tailscale/Container Manager 지원.
- 4~5대(향후 20대) STB에 미디어 서빙하는 워크로드는 가벼워서 **DS225+급(2베이 Plus)로 충분.**

---

## 4. "디스크 없음"이 뭔가 (가격이 다른 이유)
- **"디스크 없음 / 미포함 / diskless" = 본체만 판매 → 정상입니다.** NAS는 원래 디스크 별도가 표준.
- **"○○TB 합본 / HDD 포함" = 본체 + 디스크 묶음** → 그만큼 비쌈. (예: "12TB 합본"은 디스크값이 더해진 것)
- 그래서 같은 DS224+라도 **본체만(저렴) vs 디스크 합본(비쌈)** 으로 가격이 갈립니다.
- **권장: 본체(디스크 없음)만 사고, 디스크는 §2의 NAS 전용 HDD로 따로** 구매(용량·브랜드 선택 자유, 보통 더 합리적).

---

## 5. RAM(메모리)
- **Docker로 서버를 돌리면 기본 RAM은 빠듯** → **증설 권장** (전작 DS224+ 기준 기본 2GB→6GB 공식 최대였음. DS225+ 기본/최대 용량은 공식 사양표로 확인).
- 호환 메모리(시놀로지 정품 또는 호환 검증된 SODIMM)인지 확인.

---

## 6. 디스크(HDD) 선택
- **NAS 전용/24시간용 HDD**: WD **Red Plus**, Seagate **IronWolf** (일반 데스크탑용·SMR 드라이브 피하기).
- **2개로 RAID 1(미러)** — 한 개 고장나도 데이터 유지.
- 용량: 미디어가 크지 않아 **2TB~4TB면 넉넉.** 4K 영상까지 고려하면 **4TB × 2(RAID1 = 4TB 사용)** 무난.

---

## 7. 피해야 할 것 ⚠️
- **ARM CPU 모델 / DS○○j(예: DS223, DS220j)** → Docker 안 됨 → **서버 못 돌림. 절대 금지.**
- **본체만 필요한데 비싼 디스크 합본**을 모르고 사는 것.
- **스캘퍼/정가 부풀린 리셀러** 매물 (예: 정가보다 과하게 비싼 "할인" 표기) → **공식총판/정품**에서 구매.
- 일반 데스크탑 HDD를 NAS에 24시간 쓰는 것.

---

## 8. 구매 체크리스트 (제미나이/구매자용)
- [ ] 본체가 **DS225+** (또는 §3 조건 충족 모델: Intel/x86 + Docker)인가
- [ ] **디스크 미포함(본체 단독)** 상품인가, 합본인가 — 의도와 맞는가
- [ ] **공식총판/정품** + A/S 보증 되는 판매처인가
- [ ] 가격이 시세 대비 과하지 않은가 (여러 판매처 비교)
- [ ] 디스크: NAS용(Red Plus/IronWolf) **4TB × 2** 별도 담았는가
- [ ] RAM: **4GB 증설 모듈**(호환) 별도 담았는가
- [ ] (대안 모델일 경우) Intel+Docker, 3rd-party 디스크 호환, RAM 증설 모두 확인했는가

---

## 9. 요약
> **DS225+ 본체(디스크 없음) + NAS용 HDD 4TB×2(RAID1) + RAM 증설**, 공식총판 정품으로 구매. (DS225+ 사양은 공식 사양표 확인)
> "디스크 없음"은 정상(본체만), 합본은 디스크값 포함이라 비싼 것. ARM/ j시리즈는 Docker 안 되니 피할 것.
