# 2026-06-30 작업 계획 — SignagePro NAS 이전 + 현장PC/네트워크 정리

> 작성: 2026-06-29 (다음날 작업용)
> 목표: **현장 구PC(jeju-osulloc-old)에서 SignagePro를 NAS로 완전 이전**하고, 구PC는 **Desigo CC(BMS) 전용**으로 정리. 추가로 **iptime 전원/접속 불안정 하드웨어 정리.**

---

## 0. 배경 (2026-06-29 확인 사항)

- 현장 구PC = **SignagePro 서버(C:\signagepro, PM2 `signagepro-server`, v0.4.25)** + **Desigo CC(프로젝트 OSULOC, Siemens BMS)** 겸용.
- **Desigo 라이선스 소실**(ALM "no license keys") → BMS 프로젝트 안 뜸. **Siemens 영역, 이번 작업과 별개.**
- **Tailscale 컨트롤서버(`controlplane.tailscale.com` = `192.200.0.0/24`, 진짜 IP 3중 검증) 경로가 현장 회선에서만 간헐 끊김** → 원격 접속 불안정. (DNS 아님, 라우팅 문제)
- **iptime 고질**: 전원이 끊기면 STB가 접속 불가 → 랜선 뽑았다 다시 끼우면 복구되는 현상 반복.

### 이번 이전의 핵심 효과
1. SignagePro ↔ Desigo **물리적 분리** (서로 영향 차단)
2. 구PC에서 **Tailscale 제거** → 회선 경로 문제로부터 SignagePro 운영 독립
3. 운영 서버를 **NAS Docker로 단일화**

---

## 1. 사전 점검 (이전 시작 전 반드시 — 하나라도 안 되면 이전 보류)

- [ ] **NAS signagepro 서버 가동 확인**: NAS SSH 후 `sudo docker compose ps` 로 컨테이너 running, `curl -s localhost:<포트>/api/health` 200 확인. (NAS 접속: 키기반 SSH `amoreamore@jeju-osulloc`)
- [ ] **NAS DB에 현장 데이터 존재 확인**: dev-101~105 기기 등록 + 그룹/플레이리스트/미디어가 NAS에 있는지. 없으면 **미디어·스케줄 마이그레이션 먼저** (대시보드 업로드 또는 uploads 복사).
- [ ] **NAS 접속 주소·포트 결정**:
  - 같은 LAN(권장): STB → **NAS 내부IP**(예 `192.168.5.2:<포트>`). **공인IP 하어핀 회피** (현재 STB가 공인IP 하어핀 쓰면 이번에 내부IP로 교정).
  - 공인IP 경유 시: `222.116.38.23:<포트>` + **포트포워딩** 확인.
- [ ] **하트비트 포트 확인** ⚠️: APK는 server_url의 HTTP 포트로 하트비트 TCP 포트를 하드코딩 — **3000/3001 → 10081, 그 외(예 3300) → 10080**. NAS HTTP 포트가 무엇인지 확인하고, **해당 하트비트 포트(10080 또는 10081)가 NAS/포워딩에서 열려 있어야 online** 으로 뜸.
- [ ] **APK 소켓 호환 확인** ⚠️ (catch-22 방지): 현재 STB = **101/102/103 = v0.4.28, 104 = v0.4.32**. NAS(main)에 **소켓 강화(deviceId 필수)** 가 반영돼 있으면 **구버전 0.4.28이 socket.io 거부 → OTA·원격명령 차단** 위험. 
  - main 미반영이면 OK. **반영됐으면** 먼저 해당 보드 APK를 호환 버전으로 ADB/USB 직접 설치(사용자 직접) 후 이전.
- [ ] **출발 서버 생존 유지**: server_url 원격변경은 **현재 STB가 붙어있는 출발 서버(구PC signagepro)가 소켓 emit** 해야 동작. → **구PC signagepro 삭제는 STB 이전·검증이 전부 끝난 마지막에.**

---

## 2. Phase 1 — STB 주소를 NAS로 이전

- [ ] 출발 서버(구PC signagepro)에 대고 보드별로 server_url 변경:
  ```
  POST /api/devices/{deviceId}/server-url   body: { "serverUrl": "http://<NAS주소>:<포트>" }
  ```
- [ ] Device Owner 기기라 변경값이 **commit() 반영 후 kill로 재기동**돼야 적용됨. (set_server_url 픽스가 v0.4.31에 들어감 — 보드 APK 버전이 그 이상인지, 아니면 수동 재기동 필요한지 확인)
- [ ] **한 대씩** 이전 → NAS 대시보드에서 online 확인 → 다음 대. (절대 한꺼번에 X, 문제 시 한 대만 영향)
- [ ] 순서 제안: 가장 덜 중요한 보드 1대 먼저 파일럿 → OK면 나머지.

> 참고: OTA push / ADB 직접설치 = **사용자 직접 작업**(자율 금지). server_url 변경(소켓 명령)은 진행 가능.

---

## 3. Phase 2 — 실제 운영 검증

- [ ] NAS 대시보드: dev-101~105 **online + 하트비트 정상 + 미리보기 표시**.
- [ ] **콘텐츠 정상 재생**: 플레이리스트/스케줄/그룹 배정대로 화면 출력.
- [ ] 업로드/썸네일/관제 등 핵심 기능 한 번씩.
- [ ] **수 시간~하루 실제 운영 관찰** (안정성 확인). 이 단계 OK 전엔 Phase 3로 넘어가지 말 것.

---

## 4. Phase 3 — 구PC 정리 (검증 완료 후에만)

순서: **① Tailscale 삭제 → ② SignagePro 삭제**

- [ ] **① Tailscale 앱 삭제 / 원상복구**
  - 제어판 → 프로그램 제거에서 Tailscale 제거 (또는 `tailscale down` 후 서비스 중지 → 언인스톨).
  - Tailscale이 건드린 설정 원복: IPv6 바인딩 끈 게 있으면 복구(이더넷3만), 방화벽 규칙 정리.
  - ⚠️ **삭제 후 구PC 원격 경로 확인**: AnyDesk는 Tailscale과 독립이라 유지됨 → 락아웃 없음. NAS/STB 관리도 AnyDesk 또는 회선으로 가능한지 확인.
- [ ] **② SignagePro 서버 삭제**
  - `pm2 delete signagepro-server` → `pm2 save` → (자동시작 제거) `pm2 unstartup`.
  - **C:\signagepro 디렉터리 + DB + PM2 dump 백업**(외장/NAS로) 후 제거.
  - 부팅 자동시작 항목 제거 확인.
  - ⚠️ **Desigo 무영향 확인**: SignagePro 제거가 Desigo 포트/서비스와 충돌 없는지(독립 프로세스라 보통 무관) 점검.
- [ ] 결과: **구PC = Desigo CC 단독** 상태.

---

## 5. Phase 4 — 네트워크 하드웨어 정리 (iptime)

**문제:** iptime 전원 끊기면 STB 접속 불가 → 랜선 재삽입 필요(고질) + Tailscale 컨트롤 경로 간헐 끊김.

### 결정 전 진단 (권장): ONT 직결 테스트로 범인 가리기
- 노트북을 **ONT(KT 모뎀)에 직결**(iptime 우회) → 인터넷 잡힌 상태에서:
  ```powershell
  Test-NetConnection 192.200.0.101 -Port 443 -InformationLevel Quiet
  ```
  - **True (직결하면 됨)** → **iptime이 범인** → 옵션 A(교체)가 일석이조.
  - **False (직결해도 안 됨)** → **KT 상위망** 문제 → 회선사 문의 + 옵션 B 고려.

### 옵션 A — iptime 교체
- 새 공유기로 교체. 기존 구조 유지, 변경 최소. 노후/펌웨어 버그·전원복구 불안정 해소.
- 192.168.5.x 대역·포트포워딩·DHCP 설정 그대로 이관.

### 옵션 B — KT 공유기에 NAS + STB 직접 수용 (iptime 우회/제거)
- 구조 단순화, 이중 NAT·하어핀 해소 가능.
- 점검 필요: KT 공유기 **LAN 포트 수 충분**한지(NAS+STB 5대+@), **포트포워딩/DHCP 관리** 가능한지, 기존 192.168.5.x 의존 설정 영향.

> 권장 순서: **ONT 직결 테스트 → 범인 확인 → 옵션 결정.** iptime이 범인이면 교체(A)로 두 문제(전원복구·Tailscale경로) 동시 해결 가능.

---

## 6. 롤백 / 안전장치

- **STB 원복**: 구PC signagepro가 살아있는 동안엔 `POST /api/devices/:id/server-url` 로 주소를 구PC로 되돌리면 즉시 롤백. → **구PC signagepro는 검증 100% 끝까지 살려둘 것.**
- **데이터 보존**: C:\signagepro 디렉터리·DB·PM2 dump는 삭제 전 반드시 백업.
- **단계 게이트**: Phase 2 검증 통과 전엔 Phase 3(삭제) 금지.

---

## 7. 미해결 / 별개 트랙

- 🔴 **Desigo CC 라이선스 복구(ALM 비어있음)** — Siemens 설치업체 영역. 이번 이전 작업과 **무관하게 별도 진행**. (구PC가 Desigo 전용이 되면 라이선스 복구도 더 깔끔)

---

## 작업 당일 체크 요약

```
[ ] 사전점검(§1) 전부 통과
[ ] STB 1대 파일럿 이전 → NAS online/재생 확인
[ ] 나머지 STB 순차 이전
[ ] 수시간 실제 운영 안정성 확인        ← 여기 통과 전엔 삭제 금지
[ ] 구PC Tailscale 삭제 + 원복
[ ] 구PC SignagePro 백업 후 삭제(PM2 포함)
[ ] ONT 직결 테스트 → iptime/KT 판별
[ ] iptime 교체 or KT공유기 통합 결정·실행
```
