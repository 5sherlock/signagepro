# KT 라우팅 장애 문의 — 제주 오설록 현장 회선

> 작성: 2026-07-14
> 증상: **현장 회선에서만 특정 목적지 대역(`192.200.0.0/24`)에 도달하지 못함.**
> 일반 인터넷은 정상이라, "인터넷이 안 된다"가 아니라 **"특정 경로만 죽었다"** 로 접수해야 함.

---

## 0. 접수 전 30초 요약 (상담원에게 이대로 말할 것)

> "인터넷은 정상인데, **특정 IP 대역(192.200.0.101~116)으로만 TCP 연결이 안 됩니다.**
> 같은 KT 회선인 다른 사업장에서는 같은 IP에 정상 접속됩니다.
> 공유기를 교체해봤지만 동일하고, traceroute를 보면 KT 백본에서 해외망(Telia)으로
> 빠진 뒤 도달하지 못합니다. **고객 장비 문제가 아니라 경로(라우팅) 문제로 보입니다.
> 해당 목적지 대역에 대한 라우팅 확인을 요청드립니다.**"

⚠️ **"인터넷이 느려요/안 돼요"라고 하면 안 됨.** 그러면 모뎀 리셋 → 공유기 교체 →
"정상입니다"로 종결되고 실제 문제는 그대로 남습니다. 반드시 **"특정 대역 도달 불가"** 로 말할 것.

---

## 1. 회선 정보 (접수 전 채워둘 것)

| 항목 | 값 |
|---|---|
| 계약자명 / 사업자명 | (채우기) |
| 설치 주소 | 제주 오설록 (현장) |
| 인터넷 회선번호 / 고객번호 | (채우기) |
| 현장 공인 IP | **14.39.104.56** |
| 첫 KT 홉(게이트웨이) | **14.39.104.1** |
| 사내 공유기 | ipTIME (192.168.5.1) — **교체 테스트 완료, 증상 동일** |
| 서버(NAS) 내부 IP | 192.168.5.10 (Synology DS225+) |

---

## 2. 증상 (사실만)

- **정상:** 웹/일반 인터넷 (`https://www.google.com` → HTTP 200)
- **불가:** `192.200.0.101` ~ `192.200.0.116` **TCP 443 연결 전부 실패**
- 이 대역은 `controlplane.tailscale.com` (업무용 VPN 서비스의 관제 서버) 입니다.
- **DNS는 정상** — 이름은 정상적으로 위 IP로 풀립니다. **연결(라우팅) 단계에서 실패**합니다.

### 대조군 (같은 KT, 다른 회선 → 정상)
| 위치 | 회선 공인IP | `192.200.0.101:443` |
|---|---|---|
| **제주 오설록 현장** | 14.39.104.56 | ❌ **실패** |
| 사무실(개발) | 175.207.231.68 | ✅ 정상 |

→ **같은 KT인데 현장 회선에서만 실패.** 목적지 서버 장애가 아님이 확인됨.

---

## 3. 증거 — traceroute (현장 NAS에서 실행, 2026-07-14)

```
$ traceroute -n 192.200.0.101

 1  192.168.5.1        0.343 ms      ← 사내 공유기(ipTIME). 정상 통과
 2  14.39.104.1        2.217 ms      ← KT 첫 홉
 3  61.78.46.218       2.763 ms      ← KT
 4  112.188.226.73     2.802 ms      ← KT
 5  112.174.47.97      7.813 ms      ← KT
 6  112.174.86.142     9.760 ms      ← KT
 7  112.174.87.2     127.761 ms      ← ★ 여기서 지연 급증 (10ms → 128ms)
 8  213.155.141.168  130.646 ms      ← Telia (해외망)
 9  62.115.139.106   138.040 ms      ← Telia
10  62.115.139.105   161.749 ms      ← 이후 도달하지 못하고 소실
```

**요점:** 패킷이 사내 공유기를 정상적으로 빠져나가 KT 백본(2~6홉)을 잘 타다가,
**7홉에서 해외망으로 넘어간 뒤 목적지에 도달하지 못합니다.** 고객 구간(1홉)은 정상입니다.

---

## 4. 재현 명령 (상담원/기사에게 그대로 보여줄 것)

현장 아무 PC(윈도우)에서:

```powershell
# ① 이름은 정상적으로 풀린다 (DNS 정상)
nslookup controlplane.tailscale.com

# ② 그런데 그 IP로 연결이 안 된다 (라우팅 실패)
Test-NetConnection 192.200.0.101 -Port 443

# ③ 일반 인터넷은 정상 (회선 자체는 살아있음)
Test-NetConnection www.google.com -Port 443

# ④ 경로가 어디서 끊기는지
tracert -d 192.200.0.101
```

**②만 실패하고 ③은 성공**하면 이 문서의 증상이 재현된 것입니다.

리눅스/NAS라면:
```bash
curl -s -o /dev/null -w '%{http_code}\n' -m 10 https://www.google.com          # 200 (정상)
timeout 6 bash -c '</dev/tcp/192.200.0.101/443' && echo OK || echo FAIL        # FAIL
traceroute -n 192.200.0.101
```

---

## 5. 우리가 이미 배제한 것 (상담원이 되물으면 이렇게 답할 것)

| 상담원이 의심할 것 | 우리 답변 |
|---|---|
| "공유기 문제 아닌가요" | **ipTIME 공유기를 교체했으나 증상 동일.** traceroute 1홉(192.168.5.1)은 정상 통과합니다. |
| "장비 재부팅 해보셨나요" | 모뎀·공유기·서버 전부 재부팅했으나 동일합니다. |
| "DNS 문제 아닌가요" | DNS는 정상입니다(위 ① 참조). 이름은 풀리는데 **그 IP로 TCP 연결이 안 됩니다.** |
| "목적지 서버가 죽은 거 아닌가요" | **같은 KT의 다른 회선(175.207.231.68)에서는 같은 IP에 정상 접속됩니다.** |
| "인터넷은 되시죠?" | 네, 일반 인터넷은 정상입니다. **그래서 회선 장애가 아니라 특정 대역 라우팅 문제로 보입니다.** |
| "방화벽에서 막은 거 아닌가요" | 사내에서 아웃바운드를 차단한 설정이 없습니다. 필요하면 ONT 직결 테스트 결과도 제출 가능합니다. |

### (선택) ONT 직결 테스트 — 공유기를 100% 배제하는 결정적 증거
노트북을 **KT 모뎀(ONT)에 랜선 직결**(공유기 우회)하고:
```powershell
Test-NetConnection 192.200.0.101 -Port 443
```
- **여전히 실패** → 공유기 완전 배제. **KT 구간 확정.** ← 이 결과를 들고 접수하면 가장 강력합니다.
- 성공 → 공유기 문제로 회귀 (현재까지 정황상 가능성 낮음)

> ⚠️ 이 테스트는 영업 중에 하면 안 됩니다. **영업 종료 후** 진행하세요.

---

## 6. 접수 채널

| 채널 | 연락처 | 비고 |
|---|---|---|
| KT 고객센터 (일반) | **100** (국번없이) | 개인/소호 회선 |
| KT 기업고객 | **1588-0114** | 사업자 회선이면 이쪽 |
| KT 인터넷 장애신고 | 100 → 장애신고 | 기사 방문 접수 |

**접수 시 요청할 것 (문구 그대로):**

> "**목적지 대역 `192.200.0.0/24` 에 대한 라우팅 확인**을 요청합니다.
> 저희 회선(공인IP 14.39.104.56)에서만 해당 대역 TCP 443 연결이 실패하고,
> 같은 KT 다른 회선에서는 정상입니다. traceroute상 `112.174.87.2` 이후
> 해외망(Telia)으로 넘어가 도달하지 못합니다.
> **1차 상담 종결이 아니라 네트워크 운영팀 에스컬레이션을 요청드립니다.**"

⚠️ **"에스컬레이션"을 명시적으로 요구할 것.** 1차 상담선에서는 라우팅 이슈를 다루지 못합니다.
현장 기사 방문으로 끝나면 해결되지 않습니다.

---

## 7. 왜 이걸 고쳐야 하는가 (내부용 — 우선순위 판단)

- **재생·롤링에는 영향 없습니다.** STB ↔ NAS 통신은 같은 LAN(192.168.5.0/24)이라
  이 라우팅과 무관합니다. → **영업 중단 급의 장애가 아님.**
- 영향받는 것은 **원격 관리 경로**입니다:
  - NAS가 Tailscale 관제망에서 이탈 → 원격 접속·서브넷 라우팅·ACL 갱신 불가
  - 지금은 NAS **SSH가 살아 있어 우회 가능**하지만, 임시방편입니다.
- 즉 **급하진 않지만 방치하면 원격 관리가 막힙니다.** 여유 있게 접수하되 끝까지 물고 갈 것.

---

## 8. 진행 기록

| 날짜 | 내용 | 결과 |
|---|---|---|
| 2026-07-14 | 원인 규명 (NAS traceroute + 타 회선 대조) | KT 구간 라우팅 문제로 확정 |
| | ONT 직결 테스트 | (영업 종료 후 예정) |
| | KT 접수 | (예정) |
| | 에스컬레이션 | |
| | 해결 확인 | |

---

## 부록 — 관련 문서
- [2026-06-30_NAS이전_및_네트워크정리_PLAN.md](2026-06-30_NAS이전_및_네트워크정리_PLAN.md) §0, §5 — 이 문제를 처음 인지한 기록
- [DEPLOYMENT.md](DEPLOYMENT.md) — 네트워크/포트 토폴로지
