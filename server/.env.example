# 데이터베이스 경로 (SQLite)
DATABASE_URL="file:./dev.db"

# 현장 기기 인증 비밀키 (기기 설정의 deviceSecret과 일치해야 함)
DEVICE_SECRET="여기에-비밀키-입력"

# ADB 경로 (기본값: PATH에서 자동 탐색, 직접 지정 시 사용)
# ADB_PATH="C:\platform-tools\adb.exe"
