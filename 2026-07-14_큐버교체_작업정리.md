# 2026-07-14 작업 정리 — 크라이저 → 큐버 교체 완료

> 제주 오설록 현장 STB 5대를 **크라이저(os-101~105, Android 5.1)** 에서
> **큐버 QR5G-M110S(osl-1~5, Android 11)** 로 교체 완료.
> 구 보드는 물리 분리·보관. 대시보드에서도 삭제됨.

---

## 1. 오늘 완료 (요약)

| 영역 | 내용 |
|---|---|
| **회선** | Tailscale 컨트롤플레인이 **구 KT 회선(14.39.104.56)에서만 라우팅 블랙홀** → 회선 교체(**211.115.175.41**)로 해결. NAS tailnet 복귀 |
| **네트워크** | osl-1~5 유선 전환(192.168.5.11~15), wifi off, iptime DHCP 고정IP 예약, NAS 수동IP |
| **하어핀 제거** | STB `server_url` → **`http://192.168.5.10:3300`**(NAS 내부IP). 기존 공인IP 하어핀이 롤링 어긋남의 원인이었음 |
| **원격 관리** | NAS를 **Tailscale 서브넷 라우터**(192.168.5.0/24)로 → dev-home에서 현장 LAN 전체 접근 |
| **앱** | 1.1.9 → **1.2.5** (NTP 폴백 / 자동복귀 수정 + [정지] / DO 상태바·알림 차단) |
| **부팅화면** | 부트로고·부트애니 5대 적용 (`/cache/media`) |
| **교체** | 구 보드 5대 분리·삭제, osl-1~5 설치·가동 |
| **git** | `quber` 브랜치 → `main` 병합. 현재 `3d346cf` (main/dev/quber 동일) |
| **NAS** | main 반영 + Docker 이미지 재빌드 + 배포 완료 |

---

## 2. 현재 상태 (2026-07-14 밤 기준)

```
스토어    오설록
그룹      제조바 (osl-1~5)          ← "매장" 그룹은 삭제됨
기기      osl-1~5  전부 online, v1.2.5
미디어    20장 (osulloc_260629_01~20.png)
재생목록  0항목  ← ⚠️ 아직 배정 안 됨
OTA       pendingVersion 1.2.5 / apkVersion 1.0.7 (푸시 대기)
uploads   42MB (고아 mp4 16개 233MB 정리 완료)
```

### 접속 정보
| 대상 | 주소 | 계정 |
|---|---|---|
| NAS SSH | `100.66.245.72` (tailnet) / `192.168.5.10` | `amoreamore` / `Harbor-Otter-Willow12^` |
| NAS 대시보드 | `https://jeju-osulloc.tailafea2d.ts.net` 또는 `http://192.168.5.10:3300` | |
| iptime | `http://192.168.5.1` | |
| osl-1~5 ADB | tailnet `adb connect 100.x:5555` 또는 LAN `192.168.5.11~15:5555` | |

### 현장 LAN (192.168.5.0/24, GW=iptime .1)
| 기기 | IP | eth0 MAC |
|---|---|---|
| NAS | .10 | `90:09:d0:9c:8c:37` |
| osl-1 | .11 | `cc:22:18:03:46:bb` |
| osl-2 | .12 | `cc:22:18:03:46:b8` |
| osl-3 | .13 | `cc:22:18:03:46:b6` |
| osl-4 | .14 | `cc:22:18:03:46:c5` |
| osl-5 | .15 | `cc:22:18:03:46:bf` |

---

## 3. 내일 할 일

### 🔴 필수 (오늘 못 끝낸 것)

- [ ] **재생목록 배정** — 미디어 20장이 올라와 있으나 **재생목록이 비어 있음**. 지금 osl 5대는 재생할 콘텐츠가 없다.
      기존 규칙(구 보드와 동일):
      ```
      osl-1 ← 01, 06, 11, 16
      osl-2 ← 02, 07, 12, 17
      osl-3 ← 03, 08, 13, 18
      osl-4 ← 04, 09, 14, 19
      osl-5 ← 05, 10, 15, 20
      ```
      (표시 10초 / DISSOLVE 2000ms)

- [ ] **OTA 푸시** — `apkVersion` 을 1.2.5로 확정. 기기는 이미 1.2.5라 실제 설치는 없고, **자동 복구 워치독의 기준값**이 갱신된다.

- [ ] **화면 위치 검증** — **osl-1이 왼쪽 첫 화면**인지. 자리가 바뀌어 꽂혔으면 콘텐츠 순서가 어긋난다.

### 🟡 필요 시

- [ ] **비디오월** — 다시 쓰려면 동영상을 새로 올려 가공해야 한다(기존 슬라이스는 고아 정리 때 삭제됨).
      ⚠️ **비디오월 [배포]는 그룹 재생목록을 통째로 덮어쓴다**(`overwriteGroupPlaylist`).
      이미지 배정 후 비디오월을 배포하면 **이미지가 날아간다**(오늘 실제로 발생).
      → 이미지 + 슬라이스를 **미디어 스케줄링에서 함께 편성해 한 번에 저장**할 것.

- [ ] **osulloc-pi 서브넷 라우터 정리** — 사무실 LAN(192.168.0.0/24) 광고 중.
      큐버가 현장으로 옮겨간 지금은 불필요. 유지/해제 판단.

### 🟢 나중에 (크라이저 완전 은퇴 후)

- [ ] **`build.gradle` 현대화** — 크라이저(RK3229/Android 5.1) 제약이 사라졌다.
      ```
      minSdk 22 → 24+ / targetSdk 29 → 34
      armeabi-v7a only → arm64-v8a   (큐버는 RK3566 = 64비트인데 32비트로 돌고 있음)
      V1 전용 서명 → V2/V3
      ```
      ⚠️ 보관 중인 크라이저 보드를 다시 쓸 일이 없다고 확정된 뒤에.

- [ ] **`DEPLOYMENT.md` 오류 수정** — "staging 하트비트 = 10082" 는 **오류**.
      실제 `D:\signagepro\server\.env` 는 `TCP_PORT=10080`. (APK 로직상 3000/3001→10081, 그 외→10080이라
      staging과 운영이 STB 입장에서 동일 포트 = staging이 진짜 운영 리허설이 된다)

---

## 4. 오늘 배운 함정 (재발 방지)

### 앱 / 기기
- **큐버는 앱에서 root를 못 쓴다.** `/system/xbin/su` 가 `root:shell` 전용이라 앱(untrusted_app)이 exec하면 EACCES.
  → **Device Owner** 로 해결 (`dpm set-device-owner`). 원격 재부팅·always-on VPN·무음 OTA가 전부 여기 의존.
- **DO 패키지는 `am force-stop` 이 안 먹는다.** 앱 재기동은 `su 0 kill -9 <pid>`.
- **`server_url` 원격 변경은 반드시 `POST /api/devices/{id}/server-url`, body `{"url": ...}`** (필드명이 `serverUrl` 아님).
  ❌ prefs 파일을 직접 덮어쓰고 kill 하면 **HOME 런처라 즉시 재기동되어 옛 설정을 물고 뜬다.**
- **부트애니/로고는 `/cache/media`** (큐버 전용). remount 불필요. Git Bash 경로 변환 주의(`MSYS_NO_PATHCONV=1`).

### 서버 / 배포
- **OTA 자동 재배포**: 기기가 소켓 재접속할 때 `meta.apkVersion` 보다 낮으면 **자동으로 update_apk 발송**.
  **업로드만으로는 안 터진다**(`pendingVersion`만 갱신). **[OTA 푸시]가 `apkVersion`을 확정**하는 시점.
  → 구버전 기기가 살아있는 동안 푸시하면 검증 안 된 APK가 자동 설치된다.
- **`dashboard/dist` 는 git 추적 안 함.** 각 환경에서 빌드해야 한다.
  → 타임라인 UI(`45ca126`)가 **커밋 5분 전에 빌드된 dist 때문에 2주간 배포되지 않은 채** 묻혀 있었다.
- **NAS 재빌드는 5~9분** (Celeron J4125). 미디어/DB는 **볼륨**이라 재빌드와 무관(재업로드 없음).
- **Synology sshd 는 SFTP·TCP 포워딩을 막아둔다.** 큰 파일은 `ssh exec` 스트림(`cat > /tmp/x`)으로 밀 것.

---

## 5. 관련 문서
- [KT_라우팅장애_문의.md](KT_라우팅장애_문의.md) — 구 회선 라우팅 블랙홀 진단 기록(회선 교체로 해결, 접수 불필요)
- [branding/bootanimation/README.md](branding/bootanimation/README.md) — 큐버 부트로고/부트애니 배포
- [큐버_STB_입고검수_문의.md](큐버_STB_입고검수_문의.md) — 하드웨어 실측 검수
- [DEPLOYMENT.md](DEPLOYMENT.md) — 환경/포트 정본 (staging 포트 오류 수정 필요)
